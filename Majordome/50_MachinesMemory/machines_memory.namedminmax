-->> desc=Aggregate values for memory useage
--
-->> group=Machines
-->> listen=machines_memory
--->> quiet

local name = MAJORDOME_TOPIC:match("^Machines/(.-)/")

-- print(name, MAJORDOME_PAYLOAD)

return name
